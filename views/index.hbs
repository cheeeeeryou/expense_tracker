<div class="col-md-6 m-auto">
  <a href="./records/new" class="btn-outline-secondary">新增一筆支出</a>
  <div>
    <form action="/category" method="GET">
      <select class="form-select" id="categoryId" name="categoryId" onchange=" this.form.submit()">
        <option disabled selected>類別</option>
        <option value="1">飲食</option>
        <option value="2">交通</option>
        <option value="3">娛樂</option>
        <option value="4">日用品</option>
        <option value="5">其他</option>
        <option value="6">所有類別</option>
      </select>
    </form>
    <div class="d-flex justify-content-center ">
      <ul>
        {{#each records}}
        <li>
          {{this.name}}
          {{this.date}}
          {{this.category}}
          {{this.amount}}
          <a href="./records/{{ this._id }}" class="btn-outline-secondary">edit</a>
          <form action="./records/{{ this._id }}?_method=DELETE" method="POST" style="display: inline;">
            <button type="submit" class="btn btn-outline-secondary">delete</button>
          </form>
        </li>
        {{/each}}
      </ul>
    </div>
    <div>
      <p>這個月在這類型的花費是{{total}}元</p>
    </div>

  </div>